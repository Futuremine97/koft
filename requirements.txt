# Core (필수)
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9
numpy>=1.26
pillow>=10.0

# 실제 SoTA 재구성 엔진 (선택 — 없으면 자동으로 데모 엔진 폴백)
# GPU 권장. 설치:
#   pip install torch torchvision
#   pip install git+https://github.com/facebookresearch/vggt.git
# torch>=2.2
# vggt @ git+https://github.com/facebookresearch/vggt.git
